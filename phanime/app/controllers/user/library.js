import Ember from 'ember';
import Notify from 'ember-notify';

export default Ember.ObjectController.extend({
	// progress: null,

	// progressCompute: function() {

	// 	console.log(this.get('model'));
	// 	var user = this.get('model');
	// 	var libraryEntries = user.get('library_entries');

	// 	libraryEntries.then(function(libraryEntries) {
	// 		console.log(libraryEntries);
	// 	});

	// 	// if (!den) {
	// 	// 	return '0%';
	// 	// } else if (!num) {

	// 	// 	return '0%';

	// 	// } else {

	// 	// 	return ((num/den)*100).toFixed(2) + "%";

	// 	// } 		
	// }.observes('model.user.libraryEntries', 'model.user.libraryEntries.anime').on('init')
});
